# Prod profile: no localhost fallbacks. Spring refuses to start if these aren't set,
# instead of silently booting against localhost.
spring.mongodb.uri=${MONGODB_URI}
spring.data.redis.host=${REDIS_HOST}
url-shortener.base-url=${BASE_URL}
