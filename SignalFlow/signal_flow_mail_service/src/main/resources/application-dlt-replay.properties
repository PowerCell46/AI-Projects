# Run mode: republishes topic-news.notification-requested-dlt back to
# topic-news.notification-requested, then exits. See CLAUDE.md's "Running the project" for the command.
spring.kafka.listener.auto-startup=false
