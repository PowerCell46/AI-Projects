# Enum formatting

Put a blank line between each constant, not just a comma.

```java
// flag
public enum NewsStatus {
    PENDING,
    SENT,
    FAILED
}

// prefer
public enum NewsStatus {

    PENDING,

    SENT,

    FAILED
}
```
