# Copy to .env and fill in per environment.

# Base URL for API requests. Leave empty for same-origin: relative /api/...
# paths, proxied to the backend by Vite in dev and by nginx in prod. Only set
# this if the frontend is ever served from a different origin than the API.
VITE_BASE_API_URL=
